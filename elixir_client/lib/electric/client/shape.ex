defmodule Electric.Client.Shape do
  @moduledoc """
  Consumes a ShapeStream, maintains a materialised Shape value.

  Produces a stream of notification events when the value changes.
  """

  :NotImplemented
end
